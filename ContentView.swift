import SwiftUI

struct ContentView: View {
    
    @State var answer = 3
    //Did a thing with a thing 
    var body: some View {
        @State var number1 = 0
        @State var number2 = 0

    }
}
